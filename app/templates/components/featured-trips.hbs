<div class="col-sm-4">
  <div class="card" style="width: 18rem;">
    <img class="card-img-top" src="{{trip.image}}" alt="Card image cap">
    <div class="card-body">
      <h5 class="card-title">{{trip.city}}</h5>
      <p class="card-text">{{trip.description}}</p>
      <a href="#" class="btn btn-primary">View trip</a>
    </div>
  </div>
</div>
